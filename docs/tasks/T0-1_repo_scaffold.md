# T0-1 初始化仓库骨架

**状态**：✅ 已由架构师完成（本卡留档）。
**内容**：目录结构、README、requirements.txt、.gitignore、.env.example、core/app/agents/templates/tests 骨架与接口桩。
**验收**：仓库可 clone；`streamlit run app/main.py` 能起；`pytest -q` 通过 calibration 测试。
**后续 agent**：以此为基础，认领 T0-2 起的任务，勿改 `core/` 既定签名。
