# T1-4 反方/风险拆为独立角色

**目标**：把 P0 合一的 `agents/quality_review.md` 拆成两个独立角色，职责更专。
**要做什么**
- 新增 `agents/devil_advocate.md`（反方观点）：专注找反证、列证伪条件、给最坏情景。
- 新增 `agents/risk_control.md`（风险控制）：专注集中度/相关性/回撤/流动性/估值/行为风险。
- 两者沿用统一输出格式（见 docs/README / 开发文档协作协议）。
- `quality_review.md` 保留为「总编/汇总」职责（不再兼反方+风险），或标注被拆分。
**交付/验收**：两个角色文件存在、职责边界清晰、输出格式统一；在 T1-3 流程中被分别调用；均不给买卖建议；满足 Global DoD。
**依赖**：无。**下游**：T1-3。
